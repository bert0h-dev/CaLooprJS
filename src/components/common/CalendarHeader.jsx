import { h } from 'preact';
import { ToolBar } from '@/components/viewsHeader/Toolbar.jsx';

export const CalendarHeader = () => {
  return (
    <header className='container-header'>
      <ToolBar />
    </header>
  );
};
